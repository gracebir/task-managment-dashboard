/** @format */

import React from "react";

const ViewProjectModal: React.FC<{}> = ({}) => {
    return (
        <div
            className={`w-full absolute h-svh top-0 right-0 z-50 flex justify-end`}
        >
            <div className='w-[25%] h-full bg-white dark:bg-secondary shadow-lg'></div>
        </div>
    );
};

export default ViewProjectModal;
